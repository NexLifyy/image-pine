export const metadata = {
  title: "ImagePine - Background Remover",
  description: "Remove image backgrounds online entirely in your browser. Powered by local AI execution, 100% private and secure.",
};

export default function BackgroundRemoverLayout({ children }) {
  return <>{children}</>;
}
