export const metadata = {
  title: "ImagePine - Resize WebP Images",
  description: "Resize WebP images online for websites and applications to speed up page loads.",
};

export default function ResizeWebpLayout({ children }) {
  return <>{children}</>;
}
