export const metadata = {
  title: "ImagePine - About Us",
  description: "Learn more about ImagePine, our philosophy of browser-local processing, and how we protect your image privacy.",
};

export default function AboutLayout({ children }) {
  return <>{children}</>;
}
