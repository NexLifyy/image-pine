export const metadata = {
  title: "ImagePine - Image to PDF Converter",
  description: "Convert JPG, PNG, WebP, and other images into clean, formatted PDF documents.",
};

export default function ImageToPdfLayout({ children }) {
  return <>{children}</>;
}
