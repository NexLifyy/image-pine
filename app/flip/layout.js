export const metadata = {
  title: "ImagePine - Flip Images",
  description: "Flip images horizontally (mirror effect) or vertically (upside down) online for free.",
};

export default function FlipLayout({ children }) {
  return <>{children}</>;
}
