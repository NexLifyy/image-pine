export const metadata = {
  title: "ImagePine - PDF to Images Converter",
  description: "Extract pages from PDF documents and save them as separate JPG or PNG images.",
};

export default function PdfToImagesLayout({ children }) {
  return <>{children}</>;
}
