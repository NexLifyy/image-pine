export const metadata = {
  title: "ImagePine - PDF Converter",
  description: "Convert PDF documents to images (JPG/PNG), or compile images into a single PDF.",
};

export default function PdfConverterLayout({ children }) {
  return <>{children}</>;
}
